def fpm(population: list[Person]) -> list: 
    # do fpm here

    return frequent_sets


def confidence(infected_person: Person, susceptible_person: Person) -> int:
    # calculate confidence here

    return confidence

